/* 
Types for UI elements 
*/

export type OptionElement = {
    value: string,
    label: string
}

export enum EDITOR_THEME {
    LIGHT = "light",
    DARK = "vs-dark"
}

export enum EDITOR_COLOR_SCHEME {
    COBALT = "cobalt",
    VS_DARK = "vs-dark"
}
